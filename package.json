{
    "name": "appy-gas-react",
    "version": "1.0.0",
    "description": "A full-stack web application with NestJS and React.js",
    "main": "index.js",
    "scripts": {
      "start": "cd client && npm install && npm run build && cd ../server && npm install && npm start",
      "start:client": "cd client && npm install && npm start",
      "start:server": "cd server && npm install && npm start",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Vitalii Polulikh",
    "license": "MIT",
    "dependencies": {
      "express": "^4.17.1"
    }
  }